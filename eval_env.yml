name: unsup_amr_eval
channels:
  - pytorch
  - nvidia
  - conda-forge
dependencies:
  - python=3.10 # Specify the Python version for SemBLEU compatibility
  - pip

  - lightning
  - transformers
  - datasets
  - pytorch

  # Need this for t5
  - protobuf
  - sentencepiece

  # Need this for jupyter notebooks
  - ipykernel

  # Need this for progress bar
  - tqdm
  - ipywidgets

  - pip:
    - jsonargparse[signatures]

    # Need this for metrics evaluation
    - smatch